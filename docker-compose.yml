version: '3.8'
services:
  golang-htcondor:
    build: .
    image: golang-htcondor:latest
    container_name: golang-htcondor-dev
    volumes:
      - htcondor-data:/var/lib/condor
    ports:
      - "8080:8080"
      - "9618:9618"

volumes:
  htcondor-data:
